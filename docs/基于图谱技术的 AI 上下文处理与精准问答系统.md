# 基于图谱技术的 AI 上下文处理与精准问答系统

# **【需求背景】**

在教育与企业知识管理场景中，尤其是作业批改和教辅资料处理环节，随着大语言模型（LLM）的广泛应用，AI 在执行复杂分析与推理任务时，其性能高度依赖于上下文的质量。当前，为 AI 提供上下文的方式面临三大挑战：

1\.数据源异构与信息孤岛：学生的成绩表、课堂行为记录、教辅 PDF、视频等分散在不同系统中，AI 难以获取全面立体的背景信息。

2\.上下文关联弱：传统的基于关键词或简单检索的方法，难以揭示“成绩\-行为\-教师\-知识点”之间的深层关联。

3\.上下文噪音大：直接输入大量原始数据会导致信息截断和“幻觉”，缺乏面向特定任务的精准上下文生成机制。

为了解决上述问题，本项目参考“多维数据图谱化 → 宽图谱融合 → 任务驱动剪枝 → 动态更新”的技术架构，从零构建一个基于图谱的 AI 上下文处理方案。通过构建教育场景的“宽图谱”，并实现面向具体任务的动态剪枝，最终为 AI 提供高质量、自适应的上下文支持。


# **【项目周期与阶段任务】**

本次项目周期分为三个递进的阶段。



### 第一阶段：多维数据的深度图谱化构建

目标：处理 PDF 教辅文档与模拟的结构化表格数据，构建独立的领域图谱。

任务：

1. 文档图谱化：选取 1 份教辅资料（PDF，10\-20页），使用版面分析与 NER/RE 模型（或直接使用 LLM Prompt），抽取概念、公式等实体及关系，形成文档知识图谱。

2. 结构化数据事件化：基于提供的一份模拟学生成绩与作业表（CSV），将每行记录转换为“事件节点”（如考试记录），并通过外键关系连接到“学生”和“知识点”实体。

3. 将上述两类图谱数据分别导入数据库 Neo4j。



### 第二阶段：基于主数据的宽图谱融合

目标：将第一周生成的独立图谱融合为一个全局互联的“宽图谱”。

任务：

1. 实体对齐：编写实体对齐脚本。对于有唯一 ID 的实体（如学生学号）进行精确匹配；对于无唯一 ID 的实体（如从不同文档中抽取的相似知识点），使用模糊匹配进行合并。

2. 宽图谱构建：以“学生”和“知识点”作为主数据节点（“图钉”），将文档图谱和成绩事件图谱关联起来，形成高连通性的宽图谱。

3. 指标度量计算：使用相关图算法库，计算融合后图谱中核心知识点节点的 PageRank 或度中心性。



### 第三阶段：端到端问答集成与动态权重初探

目标：整合全流程，接入 LLM 完成归因分析，并尝试实现简单的权重动态更新。

任务：AI 问答集成：使用图剪枝策略生成子图，将生成的子图输入给 LLM，生成最终的分析结论与建议（如：学生知识点薄弱的原因分析）。

1. 动态更新机制：模拟一条新数据输入（如学生A最新一次测验做错了二次函数的题目），编写脚本实现图谱的增量更新，并动态削弱学生A与该知识点的“掌握”关系权重（可引入简单的时间衰减或行为削减逻辑）。

2. 项目总结：撰写项目 README，说明系统架构、运行方式及剪枝策略的效果。

****

