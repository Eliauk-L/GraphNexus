/**
 * LLM 网关应用服务 — 模型路由、配额管理、降级策略、调用审计。
 *
 * <p>注意：Java 包名中不含连字符，目录名为 {@code llmgateway}，
 * 对应文档中的「llm-gateway」模块。</p>
 */
@org.springframework.lang.NonNullApi
package com.graphnexus.application.llmgateway;
